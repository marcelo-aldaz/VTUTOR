<div class="vtutor-container">
    <div class="vtutor-header">
        <div class="vtutor-title">
            <span class="vtutor-icon">🤖</span>
            <span>VTutor - Tu Asistente de Aprendizaje</span>
        </div>
        <button id="vtutor-close-floating-btn" class="vtutor-icon-btn vtutor-close-floating" title="Cerrar/Acoplar" style="display:none;">✕</button>
        <div class="vtutor-header-actions">
            <button id="vtutor-float-btn" class="vtutor-icon-btn" title="Desacoplar ventana">🗗</button>
            <button id="vtutor-expand-btn" class="vtutor-icon-btn" title="Pantalla grande">⤢</button>
            <button id="vtutor-clear-btn" class="vtutor-clear-btn" title="Limpiar conversación">🗑️</button>
        </div>
    </div>

    {{#showadminlink}}
    <div style="padding:8px 12px;background:#fff3cd;border-bottom:1px solid #dee2e6;font-size:12px;">
        ⚙️ <a href="{{adminurl}}" target="_blank">Configurar VTutor (Administrador)</a>
    </div>
    {{/showadminlink}}
    <div id="vtutor-chat-history" class="vtutor-chat-history">
        <div class="vtutor-message vtutor-system">
            <div class="vtutor-msg-content">
                👋 ¡Hola! Soy VTutor, tu asistente de aprendizaje.
                <br><br>
                Puedo ayudarte a:
                <ul style="margin:10px 0;padding-left:20px;">
                    <li>📚 Explicar temas del curso</li>
                    <li>💡 Generar ejemplos prácticos</li>
                    <li>✅ Orientarte en tareas (sin dar respuestas)</li>
                    <li>❓ Resolver dudas conceptuales</li>
                </ul>
                <strong>¿En qué puedo ayudarte hoy?</strong>
            </div>
        </div>
    </div>

    <div id="vtutor-loading" class="vtutor-loading" style="display:none;">
        <div class="vtutor-spinner"></div>
        <span>VTutor está pensando...</span>
    </div>

    <div class="vtutor-input-area">
        <textarea id="vtutor-user-input" 
                  placeholder="Escribe tu pregunta aquí... (Ej: ¿Me puedes explicar la fotosíntesis?)" 
                  rows="2"></textarea>
        <button id="vtutor-send-btn" class="vtutor-send-btn">
            <span>📤</span>
        </button>
    </div>

    <div class="vtutor-footer">
        <small>⚠️ VTutor es una herramienta de apoyo. Siempre verifica la información con tu profesor.</small>
    </div>
</div>

<button id="vtutor-launcher-btn" class="vtutor-floating-launcher" type="button" title="Abrir VTutor" style="display:none;">🤖 VTutor</button>

<style>
.vtutor-container {
    display: flex;
    flex-direction: column;
    height: auto;
    max-height: min(72vh, 700px);
    border: 1px solid #dee2e6;
    border-radius: 8px;
    background: #ffffff;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

.vtutor-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 15px;
    border-bottom: 1px solid #dee2e6;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
}

.vtutor-title {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
    font-size: 14px;
}

.vtutor-icon {
    font-size: 18px;
}

.vtutor-clear-btn {
    background: rgba(255,255,255,0.2);
    border: none;
    color: white;
    padding: 5px 10px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
}

.vtutor-clear-btn:hover {
    background: rgba(255,255,255,0.3);
}

.vtutor-chat-history {
    overscroll-behavior: auto;
    -webkit-overflow-scrolling: touch;
    flex: 1;
    overflow-y: auto;
    padding: 15px;
    background: #f8f9fa;
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.vtutor-message {
    max-width: 85%;
    padding: 12px 16px;
    border-radius: 12px;
    font-size: 14px;
    line-height: 1.5;
    animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.vtutor-message.vtutor-user {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    align-self: flex-end;
    border-bottom-right-radius: 2px;
}

.vtutor-message.vtutor-system {
    background: #e9ecef;
    color: #212529;
    align-self: flex-start;
    border-bottom-left-radius: 2px;
}

.vtutor-msg-content strong {
    display: block;
    margin-top: 10px;
}

.vtutor-loading {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 15px;
    color: #6c757d;
    font-size: 14px;
}

.vtutor-spinner {
    width: 20px;
    height: 20px;
    border: 3px solid #dee2e6;
    border-top-color: #667eea;
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

.vtutor-input-area {
    display: flex;
    padding: 12px;
    border-top: 1px solid #dee2e6;
    background: #ffffff;
    gap: 8px;
}

#vtutor-user-input {
    flex: 1;
    padding: 10px;
    border: 1px solid #ced4da;
    border-radius: 6px;
    resize: none;
    font-family: inherit;
    font-size: 14px;
}

#vtutor-user-input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 2px rgba(102,126,234,0.25);
}

.vtutor-send-btn {
    width: 45px;
    height: 45px;
    border-radius: 6px;
    border: none;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    cursor: pointer;
    font-size: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.vtutor-send-btn:hover {
    opacity: 0.9;
}

.vtutor-send-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.vtutor-footer {
    padding: 8px 15px;
    border-top: 1px solid #dee2e6;
    text-align: center;
    background: #f8f9fa;
    color: #6c757d;
    font-size: 12px;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.vtutor-chat-history::-webkit-scrollbar {
    width: 6px;
}

.vtutor-chat-history::-webkit-scrollbar-thumb {
    background-color: #ccc;
    border-radius: 3px;
}

/* Scroll safety */
.block .vtutor-container, .block_ai_tutor .vtutor-container {
    position: relative;
}
html, body {
    scroll-behavior: auto;
}

/* ===== VTutor Floating UX v10.2 ===== */
.vtutor-header-actions {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-left: auto;
}
.vtutor-icon-btn {
    border: 1px solid #ced4da;
    background: #fff;
    border-radius: 6px;
    padding: 4px 8px;
    font-size: 14px;
    line-height: 1;
    cursor: pointer;
}
.vtutor-icon-btn:hover { background: #f8f9fa; }
.vtutor-close-floating { margin-left: 8px; }

.vtutor-floating-launcher {
    position: fixed;
    right: 16px;
    bottom: 16px;
    z-index: 1000;
    border: none;
    border-radius: 999px;
    padding: 10px 14px;
    box-shadow: 0 8px 24px rgba(0,0,0,.18);
    background: #0d6efd;
    color: #fff;
    font-weight: 600;
    cursor: pointer;
}

.vtutor-container.vtutor-floating {
    position: fixed !important;
    right: 16px;
    bottom: 72px;
    width: min(520px, calc(100vw - 24px));
    height: min(72vh, 760px);
    max-height: none;
    z-index: 1001;
    box-shadow: 0 16px 40px rgba(0,0,0,.22);
    border-radius: 12px;
}
.vtutor-container.vtutor-floating .vtutor-chat-history {
    max-height: calc(100% - 170px);
}

.vtutor-container.vtutor-fullscreen {
    position: fixed !important;
    inset: 10px;
    width: auto !important;
    height: auto !important;
    max-height: none !important;
    z-index: 1002;
    border-radius: 12px;
    box-shadow: 0 18px 44px rgba(0,0,0,.28);
}
.vtutor-container.vtutor-fullscreen .vtutor-chat-history {
    max-height: calc(100dvh - 210px);
}

.vtutor-floating-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,.2);
    z-index: 1000;
    display: none;
}

@media (max-width: 991px) {
    .vtutor-container.vtutor-floating {
        width: min(86vw, 720px);
        height: min(80dvh, 900px);
        right: 12px;
        bottom: 68px;
    }
}

@media (max-width: 767px) {
    .vtutor-floating-launcher {
        right: 12px;
        bottom: 12px;
        padding: 10px 12px;
        font-size: 14px;
    }
    .vtutor-container.vtutor-floating,
    .vtutor-container.vtutor-fullscreen {
        inset: 0 !important;
        right: 0 !important;
        bottom: 0 !important;
        width: 100vw !important;
        height: 100dvh !important;
        border-radius: 0 !important;
        max-height: none !important;
    }
    .vtutor-container.vtutor-floating .vtutor-chat-history,
    .vtutor-container.vtutor-fullscreen .vtutor-chat-history {
        max-height: calc(100dvh - 205px);
    }
    .vtutor-header-actions .vtutor-icon-btn {
        padding: 6px 8px;
    }
}

</style>